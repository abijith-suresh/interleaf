import SimplePage from "@/components/SimplePage";

export default function Changelog() {
  return (
    <SimplePage title="Changelog">
      <p>v1.0.0</p>
      <p>Initial implementation scaffold for the brev app shell, routes, Tailwind v4 token setup, and build-safe PWA baseline.</p>
    </SimplePage>
  );
}
