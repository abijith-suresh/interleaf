import SimplePage from "@/components/SimplePage";

export default function Features() {
  return (
    <SimplePage title="Features">
      <ul class="m-0 pl-5">
        <li>Local-first note storage in the browser</li>
        <li>Fast note switching with tabs and sidebar grouping</li>
        <li>Keyboard-first note creation, search, export, and browser-safe tab switching</li>
        <li>Offline-capable PWA install</li>
      </ul>
    </SimplePage>
  );
}
