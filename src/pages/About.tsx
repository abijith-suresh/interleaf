import SimplePage from "@/components/SimplePage";

export default function About() {
  return (
    <SimplePage title="About">
      <p>interleaf is a calm scratchpad for short-lived work notes.</p>
      <p>It opens directly into writing, keeps notes in the browser, and avoids accounts, cloud sync, and AI features.</p>
      <p>It is intended to be open-source and deployable as a static site.</p>
    </SimplePage>
  );
}
